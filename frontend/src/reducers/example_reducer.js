
/*const initialState = {
    amount : 0,
}

export default (state = initialState, action ){
    if(action.type === "DEPOSIT_MONEY") {
        // sera una copia del estado del objeto original (...spread operator) y quiero que aumente en 10 dolares 
        return {
            ...state,
            amount: state.amount + 10
        }
    }

}*/